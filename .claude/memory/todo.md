# todo

## Bugs

PENDING: Fix `DEFAULT_SPACING=10` vs `spacingSlider min=60` mismatch — raise default to 60 or lower slider min

## Cleanup

PENDING: Decide on `SettingsController` — implement fully (load from Injector, save + reschedule callback) or delete
PENDING: Remove TilesFX dep from pom.xml + module-info if no plans to use it

## Refactor

PENDING: Move `ConfigController` to `controllers` package for consistency with `BreakController`
